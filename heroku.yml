build:
  docker:
    web: Dockerfile
    client: ./client/Dockerfile
    server: ./server/Dockerfile

client:
  commands:
    - npm install
    - npm run build
    - npm start

server:
  commands:
    - npm install
    - node server/server.js
