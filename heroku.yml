build:
  docker:
    web: ./client/Dockerfile.client
    server: ./server/Dockerfile.server

client:
  commands:
    - npm install
    - npm run build
    - npm start

server:
  commands:
    - npm install
    - node server/server.js
